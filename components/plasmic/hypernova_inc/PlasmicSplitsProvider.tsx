/* eslint-disable */
/* tslint:disable */
// @ts-nocheck

// This code is auto-generated by Plasmic; please do not edit!
// Plasmic Project: j5F6xK8wcYv3Lu1Z2yvnp6

import * as React from "react";
import { getActiveVariation } from "@plasmicapp/react-web/lib/splits";
import { ThreeJsAstronautContextProvider } from "./PlasmicGlobalVariant__ThreeJsAstronaut"; // plasmic-import: fktnqh3Xq4lt/globalVariant

type GetActiveVariationParams = Partial<
  Parameters<typeof getActiveVariation>[0]
>;

export interface PlasmicSplitsProviderProps extends GetActiveVariationParams {
  children?: React.ReactNode;
}

export const splits = [
  {
    id: "fH5Jvnc4c1dd",
    name: "ThreeJS Astronaut",
    projectId: "j5F6xK8wcYv3Lu1Z2yvnp6",
    externalId: "ThreeJS",
    description: "Test effectiveness of interactive astronaut",
    pagesPaths: ["/"],
    type: "experiment",
    slices: [
      {
        id: "X-qBEXP-D1en",
        externalId: "static_astronaut",
        prob: 0.5,
        contents: []
      },
      {
        id: "5ivzdi3d83j1",
        externalId: "threejs_astronaut_visible",
        prob: 0.5,
        contents: [
          {
            type: "global-variant",
            projectId: "j5F6xK8wcYv3Lu1Z2yvnp6",
            groupId: "fktnqh3Xq4lt",
            group: "ThreeJsAstronaut",
            variant: "override"
          }
        ]
      }
    ]
  }
];

export function getGlobalContextValueFromVariation(
  groupId: string,
  variation: Record<string, string>
) {
  let groupValue: string | undefined = undefined;
  Object.keys(variation).forEach((variationKey: string) => {
    const [_type, splitId] = variationKey.split(".");
    const sliceId = variation[variationKey];
    const split = splits.find(
      s => s.id === splitId || s.externalId === splitId
    );
    if (split) {
      const slice = split.slices.find(
        s => s.id === sliceId || s.externalId === sliceId
      );
      if (slice) {
        const content = slice.contents.find(c => c.groupId === groupId);
        if (content) {
          groupValue = content.variant;
        }
      }
    }
  });
  return groupValue;
}

export default function PlasmicSplitsProvider(
  props: PlasmicSplitsProviderProps
) {
  const { children, traits, ...rest } = props;
  const variation = getActiveVariation({
    splits,
    traits: traits ?? {},
    ...rest
  });

  return (
    <>
      <ThreeJsAstronautContextProvider
        value={getGlobalContextValueFromVariation("fktnqh3Xq4lt", variation)}
      >
        {children}
      </ThreeJsAstronautContextProvider>
    </>
  );
}
