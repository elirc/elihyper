// This is a skeleton starter React component generated by Plasmic.
// This file is owned by you, feel free to edit as you see fit.
import * as React from 'react'
import { PlasmicNavigationBar, DefaultNavigationBarProps } from './plasmic/hypernova_inc/PlasmicNavigationBar'
import { HTMLElementRefOf } from '@plasmicapp/react-web'

// Your component props start with props for variants and slots you defined
// in Plasmic, but you can add more here, like event handlers that you can
// attach to named nodes in your component.
//
// If you don't want to expose certain variants or slots as a prop, you can use
// Omit to hide them:
//
// interface NavigationBarProps extends Omit<DefaultNavigationBarProps, "hideProps1"|"hideProp2"> {
//   // etc.
// }
//
// You can also stop extending from DefaultNavigationBarProps altogether and have
// total control over the props for your component.
export interface NavigationBarProps extends DefaultNavigationBarProps {}

function NavigationBar_(props: NavigationBarProps, ref: HTMLElementRefOf<'div'>) {
  const [isOpen, setIsOpen] = React.useState<boolean>(props.isOpen ? true : false)
  const [backgroundColor, setBackgroundColor] = React.useState<'clear' | 'black' | 'blur10'>(
    props.backgroundColor || 'clear'
  )

  // When props.isOpen is true, we should hide overflow on all elements in the tree.
  React.useEffect(() => {
    if (isOpen) {
      document.body.style.overflowY = 'hidden'
    } else {
      document.body.style.overflowY = 'auto'
    }
  }, [isOpen])

  const handleOpenToggle = () => {
    setIsOpen(!isOpen)
  }

  const handleScroll = () => {
    if (window.innerWidth > 1024) {
      const scrollY = window.scrollY || window.pageYOffset || document.documentElement.scrollTop
      if (scrollY === 0) {
        setBackgroundColor('clear')
      } else {
        setBackgroundColor('blur10')
      }
    }
  }

  React.useEffect(() => {
    window.addEventListener('scroll', handleScroll)
    return () => {
      window.removeEventListener('scroll', handleScroll)
    }
  }, [])

  return (
    <PlasmicNavigationBar
      root={{ ref }}
      {...props}
      isOpen={isOpen}
      backgroundColor={backgroundColor}
      hamburgerMenu={{
        onClick: handleOpenToggle,
      }}
    />
  )
}

const NavigationBar = React.forwardRef(NavigationBar_)
export default NavigationBar
