// This is a skeleton starter React component generated by Plasmic.
// This file is owned by you, feel free to edit as you see fit.
import * as React from 'react'
import {
  PlasmicHypernovaLinkButton,
  DefaultHypernovaLinkButtonProps,
} from './plasmic/hypernova_inc/PlasmicHypernovaLinkButton'
import { HTMLElementRefOf } from '@plasmicapp/react-web'

// Your component props start with props for variants and slots you defined
// in Plasmic, but you can add more here, like event handlers that you can
// attach to named nodes in your component.
//
// If you don't want to expose certain variants or slots as a prop, you can use
// Omit to hide them:
//
// interface HypernovaLinkButtonProps extends Omit<DefaultHypernovaLinkButtonProps, "hideProps1"|"hideProp2"> {
//   // etc.
// }
//
// You can also stop extending from DefaultHypernovaLinkButtonProps altogether and have
// total control over the props for your component.
export interface HypernovaLinkButtonProps extends DefaultHypernovaLinkButtonProps {
  buttonType?: 'button' | 'submit'
}

function HypernovaLinkButton_(props: HypernovaLinkButtonProps, ref: HTMLElementRefOf<'a'>) {
  const { onClick, destination, buttonType, ...rest } = props
  const resolvedDestination = destination ?? '#'
  const shouldInterceptNav = !destination || destination === '#'

  const handleClick = React.useCallback(
    (event: React.MouseEvent<HTMLElement>) => {
      if (shouldInterceptNav) {
        event.preventDefault()
      }

      if (buttonType === 'submit') {
        const form = (event.currentTarget as HTMLElement)?.closest('form')
        if (form) {
          if (typeof (form as HTMLFormElement).requestSubmit === 'function') {
            ;(form as HTMLFormElement).requestSubmit()
          } else {
            form.submit()
          }
        }
      }

      onClick?.(event as unknown as React.MouseEvent<HTMLButtonElement>)
    },
    [shouldInterceptNav, buttonType, onClick]
  )

  const rootOverrides = {
    ref,
    href: resolvedDestination,
    onClick: buttonType || shouldInterceptNav || onClick ? handleClick : undefined,
  }

  const buttonOverrides = buttonType ? { type: buttonType } : undefined

  // Use PlasmicHypernovaLinkButton to render this component as it was
  // designed in Plasmic, by activating the appropriate variants,
  // attaching the appropriate event handlers, etc.  You
  // can also install whatever React hooks you need here to manage state or
  // fetch data.
  //
  // Props you can pass into PlasmicHypernovaLinkButton are:
  // 1. Variants you want to activate,
  // 2. Contents for slots you want to fill,
  // 3. Overrides for any named node in the component to attach behavior and data,
  // 4. Props to set on the root node.
  //
  // By default, we are just piping all HypernovaLinkButtonProps here, but feel free
  // to do whatever works for you.

  return (
    <PlasmicHypernovaLinkButton
      root={rootOverrides as any}
      destination={resolvedDestination}
      button={buttonOverrides}
      {...rest}
    />
  )
}

const HypernovaLinkButton = React.forwardRef(HypernovaLinkButton_)
export default HypernovaLinkButton
